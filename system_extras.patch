diff --git a/libjsonpb/parse/Android.bp b/libjsonpb/parse/Android.bp
index ddb95128..660f474b 100644
--- a/libjsonpb/parse/Android.bp
+++ b/libjsonpb/parse/Android.bp
@@ -44,4 +44,9 @@ cc_library_static {
         "libbase",
         "libprotobuf-cpp-full",
     ],
+    target: {
+        windows: {
+            enabled: true,
+        },
+    },
 }
diff --git a/partition_tools/Android.bp b/partition_tools/Android.bp
index f3be9f12..5c9011bf 100644
--- a/partition_tools/Android.bp
+++ b/partition_tools/Android.bp
@@ -100,14 +100,55 @@ cc_binary {
     },
 }
 
+cc_binary_host {
+    name: "lpdumps",
+    defaults: ["lp_defaults"],
+    static_libs: [
+        "libbase",
+        "liblog",
+        "liblp",
+        "libcrypto",
+        "libcrypto_utils",
+        "libext4_utils",
+        "libsparse",
+        "libz",
+        "libprotobuf-cpp-full",
+        "libjsonpbparse",
+    ],
+    stl: "c++_static",
+    srcs: [
+        "lpdump_host.cc",
+        "lpdump.cc",
+        "dynamic_partitions_device_info.proto",
+    ],
+    proto: {
+        type: "full",
+        static: true,
+    },
+    target: {
+        linux_bionic: {
+            enabled: false,
+        },
+        windows: {
+            enabled: true,
+        },
+    },
+}
+
 cc_binary_host {
     name: "lpmake",
     defaults: ["lp_defaults"],
-    shared_libs: [
+    static_libs: [
         "libbase",
         "liblog",
         "liblp",
+        "libcrypto",
+        "libcrypto_utils",
+        "libext4_utils",
+        "libsparse",
+        "libz",
     ],
+    stl: "c++_static",
     srcs: [
         "lpmake.cc",
     ],
@@ -121,12 +162,22 @@ cc_binary_host {
 cc_binary_host {
     name: "lpadd",
     defaults: ["lp_defaults"],
-    shared_libs: [
+    static_libs: [
         "libbase",
         "liblog",
         "liblp",
         "libsparse",
+        "libcrypto",
+        "libcrypto_utils",
+        "libext4_utils",
+        "libz",
     ],
+    stl: "c++_static",
+    target: {
+        windows: {
+            enabled: true,
+        },
+    },
     srcs: [
         "lpadd.cc",
     ],
@@ -176,12 +227,22 @@ cc_binary {
 cc_binary_host {
     name: "lpunpack",
     defaults: ["lp_defaults"],
-    shared_libs: [
+    static_libs: [
         "libbase",
         "liblog",
         "liblp",
         "libsparse",
+        "libcrypto",
+        "libcrypto_utils",
+        "libext4_utils",
+        "libz",
     ],
+    stl: "c++_static",
+    target: {
+        windows: {
+            enabled: true,
+        },
+    },
     srcs: [
         "lpunpack.cc",
     ],
diff --git a/partition_tools/lpadd.cc b/partition_tools/lpadd.cc
index b2b1c74f..f11f13e7 100644
--- a/partition_tools/lpadd.cc
+++ b/partition_tools/lpadd.cc
@@ -14,7 +14,9 @@
 
 #include <getopt.h>
 #include <string.h>
+#ifndef WIN32
 #include <sysexits.h>
+#endif
 #include <unistd.h>
 
 #include <iostream>
@@ -34,9 +36,34 @@ using android::fs_mgr::ReadMetadata;
 using android::fs_mgr::UpdatePartitionTable;
 using SparsePtr = std::unique_ptr<sparse_file, decltype(&sparse_file_destroy)>;
 
+#ifdef WIN32
+static constexpr int EX_OK = 0;
+static constexpr int EX_USAGE = 64;
+static constexpr int EX_SOFTWARE = 70;
+static constexpr int EX_OSERR = 71;
+static constexpr int EX_CANTCREAT = 73;
+#endif
+
+static inline bool android_base_Realpath(const std::string& path, std::string* result) {
+#if !defined(_WIN32)
+  return android::base::Realpath(path, result);
+#else
+  result->clear();
+
+  char absPath[PATH_MAX] = {};
+  if (_fullpath(absPath, path.c_str(), PATH_MAX) == nullptr) {
+    return false;
+  }
+
+  result->assign(absPath);
+  return true;
+#endif
+}
+
 std::optional<TemporaryDir> gTempDir;
 
 static int usage(const char* program) {
+    std::cerr << "Modified version from android-15.0.0_r25 built by Rprop (https://github.com/Rprop)\n\n";
     std::cerr << program << " - command-line tool for adding partitions to a super.img\n";
     std::cerr << "\n";
     std::cerr << "Usage:\n";
@@ -190,7 +217,7 @@ bool SuperHelper::AddPartition(const std::string& partition_name, const std::str
     unique_fd raw_image_fd;
     std::optional<TemporaryFile> temp_image;
     if (!image_path.empty()) {
-        raw_image_fd.reset(open(image_path.c_str(), O_RDONLY | O_CLOEXEC));
+        raw_image_fd.reset(open(image_path.c_str(), O_RDONLY | O_BINARY | O_CLOEXEC));
         if (raw_image_fd < 0) {
             std::cerr << "open failed: " << image_path << ": " << strerror(errno) << "\n";
             return false;
@@ -230,7 +257,7 @@ bool SuperHelper::AddPartition(const std::string& partition_name, const std::str
 bool SuperHelper::OpenSuperFile() {
     auto actual_path = super_path_;
 
-    output_fd_.reset(open(actual_path.c_str(), O_RDWR | O_CLOEXEC));
+    output_fd_.reset(open(actual_path.c_str(), O_RDWR | O_BINARY | O_CLOEXEC));
     if (output_fd_ < 0) {
         std::cerr << "open failed: " << actual_path << ": " << strerror(errno) << "\n";
         return false;
@@ -247,7 +274,7 @@ bool SuperHelper::OpenSuperFile() {
 
     // PartitionOpener will decorate relative paths with /dev/block/by-name
     // so get an absolute path here.
-    if (!android::base::Realpath(actual_path, &abs_super_path_)) {
+    if (!android_base_Realpath(actual_path, &abs_super_path_)) {
         std::cerr << "realpath failed: " << actual_path << ": " << strerror(errno) << "\n";
         return false;
     }
diff --git a/partition_tools/lpdump.cc b/partition_tools/lpdump.cc
index 1d498883..cc594d86 100644
--- a/partition_tools/lpdump.cc
+++ b/partition_tools/lpdump.cc
@@ -16,11 +16,13 @@
 
 #include <getopt.h>
 #include <inttypes.h>
+#ifndef WIN32
 #include <sys/mount.h>
-#include <sys/stat.h>
 #include <sys/statvfs.h>
-#include <sys/types.h>
 #include <sysexits.h>
+#endif
+#include <sys/stat.h>
+#include <sys/types.h>
 #include <unistd.h>
 
 #include <algorithm>
@@ -46,8 +48,17 @@
 using namespace android;
 using namespace android::fs_mgr;
 
+#ifdef WIN32
+static constexpr int EX_OK = 0;
+static constexpr int EX_USAGE = 64;
+static constexpr int EX_NOINPUT = 66;
+static constexpr int EX_SOFTWARE = 70;
+static constexpr int EX_OSERR = 71;
+static constexpr int EX_CANTCREAT = 73;
+#endif
+
 static int usage(int /* argc */, char* argv[], std::ostream& cerr) {
-    cerr << argv[0]
+    cerr << "Modified version from android-15.0.0_r25 built by Rprop (https://github.com/Rprop)\n\n" << argv[0]
          << " - command-line tool for dumping Android Logical Partition images.\n"
             "\n"
             "Usage:\n"
diff --git a/partition_tools/lpmake.cc b/partition_tools/lpmake.cc
index d7085222..a67c1e35 100644
--- a/partition_tools/lpmake.cc
+++ b/partition_tools/lpmake.cc
@@ -52,6 +52,7 @@ static constexpr int O_BINARY = 0;
 /* Prints program usage to |where|. */
 static int usage(int /* argc */, char* argv[]) {
     fprintf(stderr,
+            "Modified version from android-15.0.0_r25 built by Rprop (https://github.com/Rprop)\n\n"
             "%s - command-line tool for creating Android Logical Partition images.\n"
             "\n"
             "Usage:\n"
diff --git a/partition_tools/lpunpack.cc b/partition_tools/lpunpack.cc
index 696d3f25..961329aa 100644
--- a/partition_tools/lpunpack.cc
+++ b/partition_tools/lpunpack.cc
@@ -17,7 +17,9 @@
 #include <fcntl.h>
 #include <getopt.h>
 #include <stdio.h>
+#ifndef WIN32
 #include <sysexits.h>
+#endif
 #include <sys/types.h>
 #include <unistd.h>
 
@@ -38,6 +40,30 @@ using android::base::unique_fd;
 using android::base::borrowed_fd;
 using SparsePtr = std::unique_ptr<sparse_file, decltype(&sparse_file_destroy)>;
 
+#ifdef WIN32
+static constexpr int EX_OK = 0;
+static constexpr int EX_USAGE = 64;
+static constexpr int EX_SOFTWARE = 70;
+static constexpr int EX_OSERR = 71;
+static constexpr int EX_CANTCREAT = 73;
+#endif
+
+static inline bool android_base_Realpath(const std::string& path, std::string* result) {
+#if !defined(_WIN32)
+  return android::base::Realpath(path, result);
+#else
+  result->clear();
+
+  char absPath[PATH_MAX] = {};
+  if (_fullpath(absPath, path.c_str(), PATH_MAX) == nullptr) {
+    return false;
+  }
+
+  result->assign(absPath);
+  return true;
+#endif
+}
+
 class ImageExtractor final {
   public:
     ImageExtractor(std::vector<unique_fd>&& image_fds, std::unique_ptr<LpMetadata>&& metadata,
@@ -77,6 +103,7 @@ class SparseWriter final {
 /* Prints program usage to |where|. */
 static int usage(int /* argc */, char* argv[]) {
     fprintf(stderr,
+            "Modified version from android-15.0.0_r25 built by Rprop (https://github.com/Rprop)\n\n"
             "%s - command-line tool for extracting partition images from super\n"
             "\n"
             "Usage:\n"
@@ -155,12 +182,12 @@ int main(int argc, char* argv[]) {
         // Done reading arguments; open super.img. PartitionOpener will decorate
         // relative paths with /dev/block/by-name, so get an absolute path here.
         std::string abs_super_path;
-        if (!android::base::Realpath(super_path, &abs_super_path)) {
+        if (!android_base_Realpath(super_path, &abs_super_path)) {
             std::cerr << "realpath failed: " << super_path << ": " << strerror(errno) << "\n";
             return EX_OSERR;
         }
 
-        unique_fd fd(open(super_path.c_str(), O_RDONLY | O_CLOEXEC));
+        unique_fd fd(open(super_path.c_str(), O_RDONLY | O_BINARY | O_CLOEXEC));
         if (fd < 0) {
             std::cerr << "open failed: " << abs_super_path << ": " << strerror(errno) << "\n";
             return EX_OSERR;
@@ -257,7 +284,7 @@ bool ImageExtractor::ExtractPartition(const LpMetadataPartition* partition) {
 
     // Make a temporary file so we can import it with sparse_file_read.
     std::string output_path = output_dir_ + "/" + GetPartitionName(*partition) + ".img";
-    unique_fd output_fd(open(output_path.c_str(), O_RDWR | O_CLOEXEC | O_CREAT | O_TRUNC, 0644));
+    unique_fd output_fd(open(output_path.c_str(), O_RDWR | O_BINARY | O_CLOEXEC | O_CREAT | O_TRUNC, 0644));
     if (output_fd < 0) {
         std::cerr << "open failed: " << output_path << ": " << strerror(errno) << "\n";
         return false;
